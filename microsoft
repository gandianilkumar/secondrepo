Why This Matters

Scheduled builds (cron syntax) follow the Jenkins timezone.

Build timestamps in logs use this timezone.

Prevents confusion when jobs run earlier/later than expected.

adding some data for jenkins
